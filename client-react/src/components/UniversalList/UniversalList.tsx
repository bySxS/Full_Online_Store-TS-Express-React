import React, { FC } from 'react'

interface IUniversalList {
  items: any[]
}

const UniversalList: FC<IUniversalList> = ({
  // items
}) => {
  return (
    <div>
      DFG
    </div>
  )
}

export default UniversalList
